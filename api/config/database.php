<?php
// =================================================================
// Αρχείο Ρυθμίσεων για τη Σύνδεση με τη Βάση Δεδομένων
// =================================================================

// Ορίζουμε τις σταθερές (constants) για τα στοιχεία σύνδεσης.

// Ο host του server της βάσης δεδομένων (συνήθως 'localhost' ή '127.0.0.1')
define('DB_HOST', '127.0.0.1');

// Το όνομα της βάσης δεδομένων που δημιουργήσαμε.
define('DB_NAME', 'gym_management_db');

// Ο χρήστης της βάσης δεδομένων.
// (Αντικαταστήστε το με το δικό σας username)
define('DB_USER', 'developer');

// Ο κωδικός του χρήστη της βάσης δεδομένων.
// (Αντικαταστήστε το με τον δικό σας κωδικό)
define('DB_PASS', 'Qwerty!2345');

// Η πόρτα του MySQL server (η προεπιλογή είναι 3306).
define('DB_PORT', '3306');
